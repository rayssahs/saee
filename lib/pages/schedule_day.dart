import 'package:flutter/material.dart';

class ScheduleDayPage extends StatelessWidget {
  static const String route = '/schedule-day';

  const ScheduleDayPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
